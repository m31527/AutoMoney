# AI 下單預算與行情檢查更新（2026-09-21）

這次保留原有 PAPER 帳戶、模型及風控上限；不新增服務、不增加模型呼叫頻率。

## Spark 更新：目前使用 8080 和 8083

將 `ai-budget-update.tar.gz` 傳到 Spark 的 `~/AutoMoney`，執行：

```bash
cd ~/AutoMoney
tar -xzf ai-budget-update.tar.gz
bash scripts/ai.sh start
EXIT_DASHBOARD_BIND=0.0.0.0 bash scripts/research-v2.sh start btc-eth
```

第一個啟動指令會重建原本 8080 的規則、AI、網頁服務；第二個會重建 8083 的規則、網頁服務。仍然只有原本的一個 Ollama worker。若 8083 僅供 Spark 本機使用，省略 `EXIT_DASHBOARD_BIND=0.0.0.0`。8080 沿用你現有 compose.yaml 的綁定。

更新包只有程式來源碼及本說明，不含 compose.yaml、config/ollama.env、風控設定或資料庫，因此不會覆寫你的 URL／模型／連接埠，也不會清空帳本。不要執行 `down -v`。SQLite 會增加診斷表並升級到版本 6；舊程式不支援此版本，不應直接降版重啟。

重建完成後，瀏覽器重新整理一次以載入新 JavaScript。正常運作後網頁內容仍會自動更新。下一次 AI 呼叫完成，8080 的 AI 區塊會顯示 **「預算／行情檢查 v2」** 與最近推論秒數。

檢查服務：

```bash
bash scripts/ai.sh status
bash scripts/ai.sh logs
bash scripts/research-v2.sh status btc-eth
```

## 修改內容

- 模型上下文新增 BUY／SELL 最大可申請金額、單筆上限、扣除估計費用後的現金、剩餘總部位額度及單幣配置上限。BUY 預算也納入成交滑價對淨值的影響。這些是上限，不是要求投入的金額；最後仍需通過完整風控與交易所最低額／數量步進。
- 模型提出超額建議時，保存原始建議與預算，改為觀望並標記 `AI_BUDGET_EXCEEDED`。不會把 $1,000 悄悄改成 $100 成交。
- 提出 BUY／SELL 後重新讀取所有估值幣種報價及交易對平均價。成交使用新報價。目標幣的 last／bid／ask 任一相對原報價變動超過 10 bps（0.1%）就放棄；此數值是固定政策保護，不是經回測證明的最佳門檻。
- 原訊號壽命仍受既有 max_data_age_seconds（預設 60 秒）限制；刷新報價不會讓舊訊號重新獲得 60 秒。帳戶變動、過期、更新報價失敗仍不成交。不自動追加一次模型重試。
- 每筆呼叫保存政策版本 `ai-budget-freshness-v2`、提示雜湊、推論耗時、預算、原始金額、報價檢查結果及行情年齡。歷史資料保持原樣，因此累計獲利與呼叫數包含更新前後；分析請選擇更新後區間，並核對版本統計。模型與風控設定仍凍結，這不是新開帳戶。

## 匯出資料

仍在網頁選台灣時間區間下載 ZIP，新增：

- `*/ai_diagnostics.jsonl`：每次模型呼叫的版本、預算及耗時／報價檢查。
- `summary.json`：超額建議次數、推論 p50／p95、完成檢查筆數、原始訊號過期比例及出場風控重播摘要。過期觀測可能是 HOLD，不等於錯過一筆可成交交易；呼叫失敗與風控拒絕分開計算。
- `exit_policy_replay.json`：以每一筆 SELL 當時的相同帳戶、行情、日風控及交易歷史，重算 v1／v2 的風控結果。規則策略套用各版對應的賣出金額，AI 保留部分賣出金額。
- `common_period_books`：交集區間採用實際存在的策略帳本，8083 沒有 AI 也能顯示有效重疊時間。

**出場重播不是連續組合回測。** 每筆獨立使用真實記錄的持倉；重播通過不代表成交，未重播交易所最低額／步進及成交延遲，不估算額外獲利。同一筆持倉可能在不同時間被重複評估，不能把通過次數當成交數。完整連續回測還需要行情／交易所過濾條件及初始帳本，不能由這份逐筆比較推導。

已使用 9/20 的舊資料驗證：10 次 SELL，v1 通過 2 次，v2 通過 9 次；1 次仍因冷卻時間拒絕。9/21 兩份匯出沒有 SELL，因此報告會如實顯示 0，不補造驗證事件。

## 接下來如何觀察

從更新後第一筆帶 v2 標記的 AI 呼叫起，匯出相同起訖時間的 8080／8083 資料。優先核對超額建議是否減少、推論後行情是否常過期、有效買賣是否仍被其他風控拒絕，再評估模型品質。觀望不等於程式失敗；此次沒有降低成本門檻、改用商業模型或啟用真實交易。
